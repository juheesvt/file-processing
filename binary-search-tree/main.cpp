//
// Created by juheeSVT on 2019-11-04.
//

#include "bst.hpp"

int main(){

    BinarySearchTree T;

    T.insert(T.getRoot(),40); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),11); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),77); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),33); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),20); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),90); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),99); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),70); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),88); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),80); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),66); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),10); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),22); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),30); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),44); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),55); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),50); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),60); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),100); T.inOrder(T.getRoot()); cout << endl;


    T.del(T.getRoot(), 40); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 11); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 77); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 33); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 20); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 90); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 99); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 70); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 88); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 80); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 66); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 10); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 22); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 30); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 44); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 55); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 50); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 60); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 100); T.inOrder(T.getRoot()); cout << endl;

    cout << endl;

    T.insert(T.getRoot(),40); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),11); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),77); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),33); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),20); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),90); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),99); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),70); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),88); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),80); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),66); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),10); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),22); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),30); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),44); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),55); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),50); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),60); T.inOrder(T.getRoot()); cout << endl;
    T.insert(T.getRoot(),100); T.inOrder(T.getRoot()); cout << endl;

    T.del(T.getRoot(), 100); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 60); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 50); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 55); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 44); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(), 30); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),22); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),10); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),66); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),80); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),88); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),70); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),99); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),90); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),20); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),33); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),77); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),11); T.inOrder(T.getRoot()); cout << endl;
    T.del(T.getRoot(),40); T.inOrder(T.getRoot()); cout << endl;




    return 0;
}